ul(class="nav flex-column")
    if role === "user"
        li(class="nav-item")
                a(class=(currentUrl==="dashboard" || currentUrl==="chart")?"nav-link active":"nav-link" href="/dashboard")
                    span(data-feather="home")
                    span #{__('Home')}
                    if currentUrl === "chart"
                        span(data-feather="chevron-right")
                        span #{__('Chart')}
    li(class="nav-item")
            a(class=(currentUrl==="edit-profile")?"nav-link active":"nav-link" href="/view/profile")
                span(data-feather="user")
                span #{__('My profile')}
    if role === "user"
        li(class="nav-item")
            a(class=(currentUrl === "clientAll" || currentUrl === "invoiceClient") ? "nav-link active" : "nav-link" href="/client/all")
                span(data-feather="users")
                span #{__('Clients')}
                if client || currentUrl === "invoiceClient"
                    span(data-feather="chevron-right")
                    span #{__('invoices')}
        li(class="nav-item")
                a(class=(currentUrl==="invoices")?"nav-link active":"nav-link" href="/invoice/all")
                    span(data-feather="file")
                    span #{__('All invoices')}
        li(class="nav-item")
            a(class=(currentUrl === "activities") ? "nav-link active": "nav-link" href="/activity")
                span(data-feather="activity")
                span #{__('My activities')}
        //li(class="nav-item")
            a(class="nav-link" href="#disabled")
                span(data-feather="book")
                span #{__('Projects')}
        //li(class="nav-item")
                a(class=(currentUrl==="stock")?"nav-link active":"nav-link" href="/stock/all")
                    span(data-feather="package")
                    span #{__('Stock')}
        //li(class="nav-item")
                a(class=(currentUrl==="calc")?"nav-link active":"nav-link" href="/calc/all")
                    span(data-feather="book")
                    span #{__('Calculations')}
        li(class="nav-item")
                a(class=(currentUrl === 'settings' || currentUrl === "password")?"nav-link active":"nav-link" href="/settings")
                    span(data-feather="settings")
                    span #{__('Settings')}
ul(class="nav flex-column")
    li(class="nav-item")
        a(class="nav-link",href="https://identity.basecone.com/authentication/login")
            span(data-feather="bookmark")
            span Basecone login